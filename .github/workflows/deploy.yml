on:
  push:
    branches:
      - staging


name: Deploy backend-end service
jobs:
  deploy:
    name: Deploy to Staging Environment
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Docker Build & Push Django Backend to Docker Hub
        uses: opspresso/action-docker@master
        with:
          args: --docker
        env:
          USERNAME: ${{ secrets.DOCKER_USERNAME }}
          PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          DOCKERFILE: "./docker/staging/django/Dockerfile"
          IMAGE_NAME: "afripoint/cvms_django_admin_staging_image"
          LATEST: "true"

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: 89.117.37.128
          username: root
          port: 22
          password: kzQdG6Vw4vJQx56obn885jCnuP65@
          script: |
            cd /root/cvmsadmin/cvms_django_deploy_config
            git checkout staging
            git reset --hard HEAD
            git pull origin staging
            docker stack deploy -c /root/cvmsadmin/cvms_django_deploy_config/docker-compose.yml cvmstagingdjangostack 

          
